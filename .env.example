ENCRYPTION_KEY=<64-character-hex-key>
LINKING_CODE_HASH_KEY=<long-random-secret-or-use-encryption-key>
AI_IMAGE_DAILY_LIMIT=5

# Generate a key with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
